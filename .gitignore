# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — NEVER commit real keys (SETUP.md §3)
.env
.env.*
!.env.example

# Local planning docs — never publish (gitignore has no inline comments, so the
# pattern gets its own line; the label lives here as a standalone comment)
Phase-2-Build-Kit/

# Real resumes are personal data — keep PDFs local on a public repo.
# Force-add a clearly-synthetic sample if you want one committed.
tests/sample_resumes/*.pdf

# Eval: cached predictions + bootstrapped labels from real resumes may contain PII.
# (The synthetic eval set under eval/text_resumes/ and eval/gold/ IS committed.)
eval/cache/
eval/gold_pdf/
eval/REPORT.md
